import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const storageService = () => {
  return (
    <View>
      <Text>storageService</Text>
    </View>
  )
}

export default storageService

const styles = StyleSheet.create({})